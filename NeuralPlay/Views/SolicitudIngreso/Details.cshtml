@model NeuralPlay.Models.SolicitudIngresoViewModel

@{
    ViewData["Title"] = "Detalle Solicitud";
}

<h1>Detalle Solicitud</h1>

<div>
    <dl class="row">
        <dt class="col-sm-2">Tipo</dt>
        <dd class="col-sm-10">@Model.Tipo</dd>
        <dt class="col-sm-2">Estado</dt>
        <dd class="col-sm-10">@Model.Estado</dd>
        <dt class="col-sm-2">Fecha solicitud</dt>
        <dd class="col-sm-10">@Model.FechaSolicitud.ToString("dd/MM/yyyy HH:mm")</dd>
    </dl>
</div>

<p>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Editar</a>
    <a asp-action="Index" class="btn btn-secondary">Volver</a>
</p>
