@model NeuralPlay.Models.PublicacionViewModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Nueva Publicaci�n";
    ViewBag.OcultarNavbar = true;
}

@Html.Partial("~/Views/Shared/_SidebarLeft.cshtml")
@Html.Partial("~/Views/Shared/_SidebarRight.cshtml")
@Html.Partial("~/Views/Shared/_SidebarDown.cshtml")

<style>
    /* Reset para etiquetas semánticas */
    figure, hgroup, footer, hr, fieldset, time {
        margin: 0;
        padding: 0;
    }

    fieldset {
        border: none;
        min-width: 0;
    }

    hr {
        border: none;
    }

    :root {
        --np-red: #e50a46;
        --np-transp: #21020a7a;
        --np-grey: #454545;
        --np-dark-grey: #242424;
    }

    @@font-face {
        font-family: 'Neue';
        src: url('/Recursos/NeuePower-Ultra.ttf') format('truetype');
        font-weight: normal;
        font-style: normal;
    }

    @@font-face {
        font-family: 'Lexend';
        src: url('/Recursos/Lexend-Medium.ttf') format('truetype');
        font-weight: normal;
        font-style: normal;
    }

    body {
        margin: 0;
        padding: 0;
        overflow-x: hidden;
    }

    /* Contenedor principal - solo fondo */
    .main-container {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100vh;
        background: url('/Recursos/WALLPAPER_GRIS.png') no-repeat center center fixed;
        background-size: cover;
        z-index: 1;
    }

    /* Contenedor de contenido - entre las barras */
    .content-container {
        position: absolute;
        left: 11%;
        top: 0;
        width: 83%; /* 100% - 11% (izq) - 6% (der) */
        min-height: 100vh;
        z-index: 2;
        padding: 60px 40px;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    /* T�tulo principal */
    .page-header {
        margin-bottom: 30px;
        text-align: center;
    }

    .page-title {
        font-family: 'Neue', sans-serif;
        font-size: 6rem;
        color: white;
        text-transform: uppercase;
        text-shadow: 0 4px 10px rgba(0, 0, 0, 0.8);
        margin: 0 0 15px 0;
        letter-spacing: 3px;
        line-height: 1;
    }

    .page-subtitle {
        font-family: 'Lexend', sans-serif;
        font-size: 1.5rem;
        color: var(--np-red);
        margin: 0;
        font-weight: 600;
    }

    /* Card del formulario */
    .form-card {
        background: rgba(36, 36, 36, 0.95);
        border-radius: 10px;
        padding: 40px;
        margin-bottom: 30px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
        max-width: 1000px;
        width: 100%;
    }

    /* Estilos especificos de formulario */

    .form-textarea {
        font-family: 'Lexend', sans-serif;
        font-size: 1.2rem;
        width: 100%;
        padding: 15px 20px;
        background: rgba(69, 69, 69, 0.8);
        border: 2px solid var(--np-grey);
        border-radius: 8px;
        color: white;
        transition: all 0.3s ease;
        resize: vertical;
        min-height: 200px;
    }

    .form-textarea:focus {
        outline: none;
        border-color: var(--np-red);
        background: rgba(69, 69, 69, 1);
        box-shadow: 0 0 10px rgba(229, 10, 70, 0.3);
    }

    .form-textarea::placeholder {
        color: #999;
        font-style: italic;
    }

    /* Mensajes de validaci�n */
    .text-danger {
        font-family: 'Lexend', sans-serif;
        font-size: 1rem;
        color: #ff4a7d;
        margin-top: 5px;
        display: block;
    }

    .validation-summary-errors {
        background: rgba(229, 10, 70, 0.2);
        border: 2px solid var(--np-red);
        border-radius: 8px;
        padding: 15px 20px;
        margin-bottom: 20px;
    }

    .validation-summary-errors ul {
        margin: 0;
        padding-left: 20px;
        list-style: none;
    }

    .validation-summary-errors li {
        font-family: 'Lexend', sans-serif;
        font-size: 1.1rem;
        color: white;
        margin-bottom: 5px;
    }

    .validation-summary-errors li::before {
        content: '? ';
        margin-right: 8px;
    }

    /* Botones */
    .form-actions { justify-content: center; }

    .btn-submit {
        font-family: 'Lexend', sans-serif;
        font-size: 1.3rem;
        padding: 15px 50px;
        background: var(--np-red);
        color: white;
        border: 2px solid var(--np-red);
        border-radius: 25px;
        cursor: pointer;
        transition: all 0.3s ease;
    }

    .btn-submit:hover {
        background: transparent;
        color: var(--np-red);
        transform: scale(1.05);
    }

    .btn-cancel {
        font-family: 'Lexend', sans-serif;
        font-size: 1.3rem;
        padding: 15px 50px;
        background: transparent;
        color: white;
        border: 2px solid white;
        border-radius: 25px;
        cursor: pointer;
        transition: all 0.3s ease;
        text-decoration: none;
        display: inline-block;
    }

    .btn-cancel:hover {
        background: white;
        color: var(--np-dark-grey);
    }

    /* Nota informativa */
    .form-note {
        font-family: 'Lexend', sans-serif;
        font-size: 1rem;
        color: #aaa;
        font-style: italic;
        margin-top: 5px;
    }

    /* Responsivo - Tablet (581px a 1200px) */
    @@media (min-width: 581px) and (max-width: 1200px) {
        .content-container {
            padding-left: 100px !important;
            padding-right: 100px !important;
            padding-top: 40px !important;
            padding-bottom: 100px !important;
        }

        .page-title {
            font-size: 4rem !important;
        }

        .page-subtitle {
            font-size: 1.2rem !important;
        }

        .form-card {
            padding: 30px !important;
        }
    }

    /* Responsivo - Mobile (≤580px) */
    @@media (max-width: 580px) {
        .content-container {
            padding-left: 15px !important;
            padding-right: 15px !important;
            padding-top: 30px !important;
            padding-bottom: 120px !important;
        }

        .page-title {
            font-size: 3rem !important;
        }

        .page-subtitle {
            font-size: 1rem !important;
        }

        .form-card {
            padding: 20px !important;
        }

        .form-label {
            font-size: 1.2rem !important;
        }

        .form-textarea {
            font-size: 1rem !important;
            min-height: 150px !important;
        }

        .btn-submit, .btn-cancel {
            font-size: 1.1rem !important;
            padding: 12px 30px !important;
        }
    }
</style>

<!-- Contenedor principal: SOLO FONDO -->
<div class="main-container"></div>

<!-- Contenedor de contenido: TODO EL CONTENIDO -->
<section class="content-container">
    <header class="page-header">
        <hgroup>
            <h1 class="page-title">Nueva Publicación</h1>
            @if (!string.IsNullOrEmpty(Model.NombreComunidad))
            {
                <p class="page-subtitle">En la comunidad: @Model.NombreComunidad</p>
            }
        </hgroup>
    </header>

    <section class="form-card">
        <form asp-action="Create">
            <aside asp-validation-summary="ModelOnly" class="validation-summary-errors"></aside>
            
            @* Campo oculto para mantener el IdComunidad *@
            @if (Model.IdComunidad.HasValue)
            {
                <input type="hidden" asp-for="IdComunidad" />
            }
            
            <fieldset class="form-group">
                <label asp-for="contenido" class="form-label">Contenido</label>
                <textarea asp-for="contenido" 
                          class="form-textarea" 
                          placeholder="Escribe el contenido de tu publicación..."></textarea>
                <span asp-validation-for="contenido" class="text-danger"></span>
                <p class="form-note">Comparte tus ideas, logros o preguntas con la comunidad</p>
            </fieldset>

            <nav class="form-actions">
                <button type="submit" class="btn-submit">Publicar</button>
                @if (Model.IdComunidad.HasValue)
                {
                    <a asp-controller="Comunidad" asp-action="Details" asp-route-id="@Model.IdComunidad" class="btn-cancel">Cancelar</a>
                }
                else
                {
                    <a asp-action="Index" asp-controller="Home" class="btn-cancel">Cancelar</a>
                }
            </nav>
        </form>
    </section>
</section>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

