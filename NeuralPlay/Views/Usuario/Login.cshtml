@model NeuralPlay.Models.LoginViewModel

@{
    ViewData["Title"] = "Iniciar Sesión";
    Layout = null; // Sin layout para diseño de pantalla completa
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - NeuralPlay</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <style>
        /* === COLORES DEFINIDOS === */
        :root {
            --np-red: #e50a46;
            --np-transp: #21020a7a;
            --np-grey: #454545;
            --np-dark-grey: #242424;
        }
        @@font-face {
            font-family: 'Neue';
            src: url('/Recursos/NeuePower-Ultra.ttf') format('truetype');
            font-weight: normal;
            font-style: normal;
        }
        @@font-face {
            font-family: 'Lexend';
            src: url('/Recursos/Lexend-Medium.ttf') format('truetype');
            font-weight: normal;
            font-style: normal;
        }

        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Segoe UI', sans-serif;
            overflow-x: hidden; /* Evita scroll horizontal */
        }

        .login-wrapper {
            min-height: 100vh;
            width: 100%;
            display: flex;
            flex-direction: row;
            position: relative;
        }

        /* === LADO IZQUIERDO (3/4) === */
        .left-panel {
            background: url('/Recursos/WALLPAPER.png') no-repeat center center;
            background-size: cover;
            position: relative;
            display: flex;
            flex-direction: column;
            width: 100%;
        }

        /* Capa opcional para mejorar contraste si el wallpaper es muy claro/oscuro
           Quítalo si quieres ver la imagen pura */
        .left-panel::before {
            content: "";
            position: absolute;
            top: 0; left: 0; right: 0; bottom: 0;
            background: rgba(255, 255, 255, 0.1); /* Sutil tinte */
            z-index: 1;
            pointer-events: none; /* Permite hacer clic a través de esta capa */
        }

        .left-content {
            position: absolute;
            left: 0;
            height: 100%;
            width: 67%;
            z-index: 2;
            pointer-events: none; /* El contenido de la izquierda no bloquea clics */
            text-align: center;
            display: flex;
            flex-direction: column;
            justify-content: center; /* Centrado vertical */
            align-items: center;     /* Centrado horizontal */
        }

        .brand-logo {
            max-width: 1000px; /* Ajusta según el tamaño real de tu imagen */
            width: 100%;
            height: auto;
            margin-bottom: 20px;
            /* Filtro de sombra suave para que destaque sobre el wallpaper */
            filter: drop-shadow(0 2px 4px rgba(0,0,0,0.3));
        }

        .welcome-text {
            color: black; /* Texto negro solicitado */
            font-size: 2rem;
            font-weight: 600;
            font-family: 'Lexend', sans-serif;
            font-style: italic;
            padding: 15px;
            border-radius: 10px;
            display: inline-block;
            text-align: left;
            margin: 0 100px
        }

        /* === LADO DERECHO (1/4) === */
        .right-panel {
            background-color: var(--np-transp); /* #242424 */
            color: white;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 3rem;
            position: absolute;
            height: 100%;
            width: 33%;
            right: 0;
            z-index: 3; /* Asegura que el panel de login esté por encima */
            pointer-events: auto; /* Habilita la interacción en este panel */
        }

        .login-title {
            color: white;
            font-size: 3.5rem;
            font-weight: bold;
            margin-bottom: 2rem;
            text-align: center;
            font-family: 'Neue', sans-serif;
        }

        /* Estilos de Inputs */
        .form-label {
            color: #ccc;
            font-size: 0.9rem;
        }

        .form-control-np {
            background-color: white;
            border: none;
            border-radius: 4px;
            padding: 10px;
            color: var(--np-grey);
            font-family: 'Lexend', sans-serif;
            font-size: 1.5rem;
        }
        
        .form-control-np:focus {
            box-shadow: 0 0 0 3px var(--np-red);
        }

        /* Botón Personalizado */
        .btn-neural {
            background-color: white;
            color: var(--np-red); /* #e50a46 */
            border: 1px solid black;
            font-weight: bold;
            padding: 10px;
            width: 100%;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-family: 'Neue', sans-serif;
            font-size: 2rem;
        }

        .btn-neural:hover {
            background-color: #f0f0f0;
            color: #c4083c;
        }

        /* Textos del Pie */
        .login-footer {
            margin-top: 2rem;
            text-align: center;
            font-size: 1.5rem;
            >span{
                font-family: 'Lexend', sans-serif;
            }
        }

        .text-register {
            color: var(--np-red); /* #e50a46 */
            text-decoration: none;
            font-family: 'Neue', sans-serif;
            text-decoration: underline;

        }
        
        .text-register:hover {
            text-decoration: underline;
            color: #ff4a7d;
        }

        .forgot-password {
            display: block;
            margin-top: 1rem;
            color: #aaa;
            text-decoration: none;
            font-size: 0.85rem;
        }

        .forgot-password:hover {
            color: white;
        }

        /* Mensajes de Validación */
        .text-danger {
            color: var(--np-red) !important;
            font-size: 0.8rem;
            margin-top: 5px;
            display: block;
        }
    </style>
</head>
<body>

    <div class="container-fluid p-0">
        <div class="login-wrapper">
            
            <div class="left-panel">
                <div class="left-content">
                    <img src="~/Recursos/WORDMARK_BLANCA.png" alt="NeuralPlay Logo" class="brand-logo" />
                    
                    <div class="welcome-text">
                        Bienvenido a NeuralPlay, la comunidad para que los gamers conecten entre sí
                    </div>
                </div>
            </div>

            <div class="right-panel">
                <h2 class="login-title">Iniciar Sesión</h2>

                <form asp-action="Login" method="post">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger p-2 mb-3" style="background:transparent; border: 1px solid var(--np-red); color: white;"></div>

                    <div class="mb-4">
                        <input asp-for="Email" class="form-control form-control-np" placeholder="Usuario/Email" />
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>

                    <div class="mb-5">
                        <input asp-for="Password" type="password" class="form-control form-control-np" placeholder="Contraseña" />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <button type="submit" class="btn btn-neural">
                        ENTRAR
                    </button>

                    <div class="login-footer">
                        <span>¿No tienes cuenta?</span>
                        <a asp-controller="Usuario" asp-action="Create" class="text-register ms-1">Regístrate</a>
                        
                    </div>
                </form>
            </div>

        </div>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <partial name="_ValidationScriptsPartial" />
</body>
</html>