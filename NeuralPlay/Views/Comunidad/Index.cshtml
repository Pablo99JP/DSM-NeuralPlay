@model IEnumerable<NeuralPlay.Models.ComunidadViewModel>

@{
    ViewData["Title"] = "Comunidades";
}

<h1>@ViewData["Title"]</h1>

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}

<p>
    <a asp-action="Create" class="btn btn-primary">Crear Nuevo</a>
</p>
<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdComunidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaCreacion)
            </th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model ?? Enumerable.Empty<NeuralPlay.Models.ComunidadViewModel>()) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdComunidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @item.FechaCreacion.ToString("dd/MM/yyyy")
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdComunidad" class="btn btn-sm btn-outline-secondary">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdComunidad" class="btn btn-sm btn-outline-info">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.IdComunidad" class="btn btn-sm btn-outline-danger">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
